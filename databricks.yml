bundle:
  name: databricks_bc_project

include:
  - resources/*.yml

variables:
  catalog:
    description: Unity Catalog catalog used by the learning pipeline
    default: workspace
  schema:
    description: Schema used by the learning pipeline
    default: databricks_bc_project
  cluster_id:
    description: Existing Databricks cluster used to run the learning workflow

targets:
  dev:
    default: true
    mode: development
    workspace:
      root_path: ~/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
